meta {
  name: List contract transactions
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/query/contracts/{{contractId}}/transactions?limit={{limit}}&before={{beforeCursor}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}
